<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taxiapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaxiApplication">
        <activity android:name=".TermsAndCodition"></activity>
        <activity android:name=".Promocode" />
        <activity android:name=".RidesDetails" />
        <activity android:name=".ProfileDetails" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ForgetPassword" />
        <activity
            android:name=".HomeScreen"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".Registration" />
        <activity android:name=".Loginpage" />
        <activity android:name=".FirstPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>